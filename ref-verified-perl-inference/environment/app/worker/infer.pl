#!/usr/bin/perl
use strict;
use warnings;

# STUB. Reads a JSON inference request on STDIN and must write a JSON
# response to STDOUT. See /app/contract/CONTRACT.md for the feature
# contract, /app/config/model.lock.json for pinned model refs, and
# /app/model.git for the model repository. Not yet implemented.

print STDOUT qq({"error":"not_implemented","detail":"infer.pl has not been implemented yet"});
exit 1;
